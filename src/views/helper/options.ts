/* ObjectsView */

// Each object has the terrain tile it belongs to filled underneath
export const DRAW_TILE_UNDERNEATH: boolean = false;

// Each object has the edges of its bounding box drawn
export const DRAW_BOUNDING_BOXES: boolean = false;

/* TerrainView */

// Colors the current terrain the Player is located in
export const HIGHLIGHT_CURRENT_CHUNK: boolean = false;

/* PlayerView */

// The edges of the Player's hitbox are drawn
export const DRAW_PLAYER_HITBOX: boolean = false;
